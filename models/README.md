# Models (downloaded on first run, not committed)

| Model | Size | Role |
|---|---|---|
| `distil-small.en` Q5 | ~100–170 MB | Default ASR (FR-VI-05) |
| `tiny.en` | ~75 MB | Fallback on weak hardware |
| `distil-large-v3` | ~600MB+ | Optional upgrade (FR-VI-07) |
| Silero VAD | ~2 MB | Speech boundaries |
| TinyTTS ONNX | ~5 MB | Default TTS |

Downloads are resumable with checksum verification (NFR-13).
