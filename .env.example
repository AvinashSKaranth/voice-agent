# Copy to .env and fill in. NEVER commit .env (BR-04: secrets in OS keychain or env only).
# Key names only — no values are ever logged by the runtime or tests.
PROVIDER=openrouter
OPENROUTER_API_KEY=
NVIDIA_NIM_API_KEY=
LLM_MODEL=openai/gpt-4o-mini
VOICE_AGENT_TOKEN=
PORT=3790
